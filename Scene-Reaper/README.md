# Scene-Reaper
A lightweight tool for detecting and extracting scenes from video files.

## Features
- Automatic scene detection using threshold-based methods
- Batch processing support
- Configurable detection parameters

## Installation
bash
pip install -e .


## Usage
bash
scene-reaper --input input_video.mp4 --output scenes --threshold 25.0

