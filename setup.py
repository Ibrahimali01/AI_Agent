from setuptools import setup, find_packages

setup(
    name="myapp",
    version="1.0.0",
    description="My application",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)